/*
import { ExecutionContext, createParamDecorator } from '@nestjs/common';

type UserIdentifier = number | string;
 export const User = createParamDecorator(
  (_data: UserIdentifier, ctx: ExecutionContext) => {
    return null;
  },
);
 */
